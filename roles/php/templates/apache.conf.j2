<VirtualHost _default_:80>
	DocumentRoot /vagrant

	<filesMatch "\.(js|html|css|php)$">
		SetOutputFilter DEFLATE
	</filesMatch>

	<Directory /vagrant>
		AllowOverride All
		Require all granted
	</Directory>
</VirtualHost>
